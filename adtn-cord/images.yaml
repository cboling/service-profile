tosca_definitions_version: tosca_simple_yaml_1_0

imports:
   - custom_types/xos.yaml

description: autogenerated nodes file

topology_template:
  node_templates:
    adtran_pod_v2:
        type: tosca.nodes.Site

    cirros:
      type: tosca.nodes.Image
      properties:
         disk_format: QCOW2
         container_format: BARE

    radius-server-ubuntu:
      type: tosca.nodes.Image
      properties:
         disk_format: QCOW2
         container_format: BARE

    vAOS-firewall:
      type: tosca.nodes.Image
      properties:
         disk_format: QCOW2
         container_format: BARE

    AdtnDeployment:
      type: tosca.nodes.Deployment
      properties:
          flavors: m1.large, m1.medium, m1.small, m1.vaos
      requirements:
          - image:
              node: cirros
              relationship: tosca.relationships.SupportsImage
          - image:
              node: radius-server-ubuntu
              relationship: tosca.relationships.SupportsImage
          - image:
              node: vAOS-firewall
              relationship: tosca.relationships.SupportsImage
